using UnityEngine;

public class CardSlot : MonoBehaviour
{

    private int acumulatingValue = 0;

    //Will probably need a reference to the transform? Or to the card it is attached to then?
    

    public void CheckToFlipCard() {
        //Should check if it has a card, and if it does, check if it should be flipped then?
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
